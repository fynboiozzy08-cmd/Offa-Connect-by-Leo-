# Offa Connect by Leo

Premium Offa community dating app built with Expo React Native and Supabase.

## Web

- Local: `npm install` then `npm run web`
- Production: `npx expo export --platform web`
- Vercel output: `dist`
